---
phase: 01.5-worker-rust-migration-inserted
plan: 05
subsystem: docs
tags: [rust-worker, workers-rs, event-schema, ts-rs, documentation]

requires:
  - phase: 01.5-worker-rust-migration-inserted
    provides: Rust event-schema crate, Rust Worker scaffold, live Hyperdrive db-ping, Rust-only cut-over
provides:
  - Canonical docs aligned to Rust-only Worker and crates/event-schema
  - Requirements rewritten for BACK-04, INGE-03, INGE-07, INGE-08, INGE-10, INGE-11, SPEC-04, SPEC-05, AGGR-12
  - AGGR-12 amendment in REQUIREMENTS.md and ROADMAP.md: no R2 enum manifest in v1
  - Research/spec docs pointing at crates/event-schema and apps/web/src/generated
affects: [phase-02-ingest-backbone, phase-03-rust-cli, phase-04-dashboard, phase-05-launch]

tech-stack:
  added: []
  patterns: [Rust-only Worker docs, ts-rs generated binding source, upstream tokio-postgres query_typed_one guidance]

key-files:
  created:
    - .planning/research/STACK.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/PITFALLS.md
  modified:
    - CLAUDE.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - spec/event-schema.md
    - spec/pow-v1.md
    - spec/payload-canonical.md

key-decisions:
  - "Documentation follows the 01.5-03 outcome: upstream tokio-postgres works through Hyperdrive when using query_typed_one; the earlier devsnek fork assumption is not current for this smoke path."
  - "AGGR-12 is amended per D-27/D-28: no enums.json is published to R2 in v1; the SPA imports enum sets from apps/web/src/generated/."
  - "Research docs were structurally rewritten because keeping historical Worker-stack sections would fail the stale-reference threat gates."

patterns-established:
  - "Canonical type source is crates/event-schema; frontend type/enum source is apps/web/src/generated/."
  - "Rust Worker docs name workers-rs 0.8.1 first-class Hyperdrive and RateLimiter bindings."

requirements-completed: [BACK-04, INGE-03, INGE-07, INGE-08, INGE-10, INGE-11, SPEC-04, SPEC-05, AGGR-12]

duration: 42min
completed: 2026-04-30
---

# Phase 01.5 Plan 05: Documentation Rewrites Summary

**Canonical docs now reflect a Rust-only Worker, crates/event-schema as shared type source, ts-rs bindings for the SPA, and the 01.5-03/04 cut-over outcomes**

## Performance

- **Duration:** 42 min
- **Started:** 2026-04-30T22:23:00Z
- **Completed:** 2026-04-30T23:05:33Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Rewrote CLAUDE.md and PROJECT.md to describe workers-rs 0.8.1, worker-build, crates/event-schema, crates/pow, ts-rs, first-class Hyperdrive/RateLimiter bindings, and the upstream tokio-postgres `query_typed_one` outcome.
- Rewrote all required REQUIREMENTS.md IDs and amended AGGR-12 so the frontend consumes enum sets from `apps/web/src/generated/`; no R2 enum manifest is published in v1.
- Replaced stale research-stack prose with current Rust Worker architecture, stack, and pitfalls docs.
- Updated ROADMAP.md Phase 4 success criterion #2 and spec docs to point at `crates/event-schema` and `apps/web/src/generated/`.

## Task Commits

1. **Task 1: Project stack docs** - `547ef96` (docs)
2. **Task 2: Requirements rewrite** - `6c6ffa1` (docs)
3. **Task 3: Research, roadmap, and spec docs** - `3e14c0e` (docs)

## Files Created/Modified

- `CLAUDE.md` - Rust Worker stack, PoW gate, architecture, and logging boundary.
- `.planning/PROJECT.md` - Constraints and decisions for Rust Worker, shared types, and R2 enum drop.
- `.planning/REQUIREMENTS.md` - SPEC/BACK/INGE/AGGR rewrites for Rust Worker and ts-rs source of truth.
- `.planning/ROADMAP.md` - Phase 1.5 current state and Phase 4 SC#2 amendment.
- `.planning/research/STACK.md` - Current Rust Worker stack research.
- `.planning/research/ARCHITECTURE.md` - Current Rust Worker architecture and component boundaries.
- `.planning/research/PITFALLS.md` - Current pitfalls, including post-01.5-03 Hyperdrive resolution status.
- `spec/event-schema.md` - Rust source of truth and generated TypeScript binding pointers.
- `spec/pow-v1.md` - Rust-only verifier consumer language.
- `spec/payload-canonical.md` - `serde_jcs = "0.2"` and RFC 8785 conformance gate.

## Decisions Made

- Followed the actual 01.5-03 deploy outcome: upstream `tokio-postgres` works for the smoke path with `query_typed_one`.
- Treated `apps/web/src/generated/` as the frontend enum/type source and removed R2 enum-manifest language from v1 docs.
- Kept STATE.md untouched because plan 01.5-05 explicitly marks state updates as orchestrator-side follow-up.

## Verification

- `cargo test -p pow --locked` passed.
- `cargo run -p xtask --quiet --locked -- gen-fixtures --check` passed.
- `cargo test --features ts-export -p event-schema --locked` passed.
- `git diff --exit-code apps/web/src/generated/` passed.
- `git grep -nE 'console_log!.*(event_id|nonce|cf-connecting-ip|elapsed|password|conn_string)' apps/worker/ crates/` returned no forbidden matches.
- `cd apps/worker && worker-build --release && test $(wc -c < build/worker/index.wasm) -lt 2621440` passed; size was 872335 bytes.
- Final stale-reference checks passed for `spec/enums.json`, `apps/worker/src/pow.ts`, `vitest run pow`, `Hono`, `mysql2`, and `zod` outside historical `.planning/phases/` records.
- PoW invariant spot-check passed for `72-byte`, `K=22`, `60s`, `payload_hash`, `SHA-256`, and `HMAC-SHA256`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Expanded stale-reference cleanup beyond named requirement IDs**
- **Found during:** Task 2 and Task 3 verification.
- **Issue:** Stale references also existed in SPEC-03, BACK-06, BACK-07, and PROJECT.md active checklist text. Leaving them would fail T-01.5-19/T-01.5-20.
- **Fix:** Rewrote those lines to point at `crates/event-schema`, `apps/web/src/generated/`, and the Rust-only CI gate.
- **Files modified:** `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`
- **Verification:** Final stale-reference grep returned no non-historical matches.
- **Committed in:** `6c6ffa1`, `3e14c0e`

**2. [Rule 2 - Missing Critical] Structurally rewrote research docs**
- **Found during:** Task 3.
- **Issue:** STACK.md, ARCHITECTURE.md, and PITFALLS.md contained broad historical Worker-stack sections. Targeted substitutions would have left stale Hono/mysql2/zod-style references and misleading Phase 2 guidance.
- **Fix:** Replaced those research docs with current Rust Worker stack, architecture, and pitfalls content.
- **Files modified:** `.planning/research/STACK.md`, `.planning/research/ARCHITECTURE.md`, `.planning/research/PITFALLS.md`
- **Verification:** Final stale-reference grep returned no non-historical matches.
- **Committed in:** `3e14c0e`

---

**Total deviations:** 2 auto-fixed (2 missing critical)
**Impact on plan:** Both fixes were required to satisfy the doc-sweep threat model. No source code or behavior changed.

## Issues Encountered

- `gsd-sdk` was unavailable in this workspace, so SDK-driven state automation was not run.
- `.planning/research/*` files are ignored by repo rules; they were force-added individually because the plan explicitly listed them as canonical docs to update.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Threat Flags

None. This plan changed documentation only and introduced no new network endpoint, auth path, file access pattern, or schema boundary.

## Next Phase Readiness

Phase 2 can now plan against Rust Worker assumptions: `workers-rs 0.8.1`, first-class `Hyperdrive` and `RateLimiter` bindings, upstream `tokio-postgres` with `query_typed_*` through Hyperdrive, `crates/pow` for verification, and `crates/event-schema` for validation and canonical bytes.

## Self-Check: PASSED

- Summary file exists on disk.
- Research docs listed in this summary exist on disk.
- Task commits `547ef96`, `6c6ffa1`, and `3e14c0e` exist in git history.
- Stub scan found no TODO/FIXME/placeholder patterns in the files changed by this plan.

---
*Phase: 01.5-worker-rust-migration-inserted*
*Completed: 2026-04-30*
