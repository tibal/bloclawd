---
phase: 01.5-worker-rust-migration-inserted
plan: 03
subsystem: backend
tags: [rust, workers-rs, hyperdrive, tokio-postgres, planetscale, smoke-test]

requires:
  - phase: 01.5-worker-rust-migration-inserted
    provides: Rust Worker scaffold, Hyperdrive binding declaration, worker-build pipeline
provides:
  - Live `GET /db-ping` staging smoke route returning `{"ok":true}` through Cloudflare Hyperdrive
  - Per-request `tokio-postgres` connect_raw + spawn_local connection idiom for Phase 2 ingest
  - Verified upstream tokio-postgres `query_typed_one` workaround for Hyperdrive pooler compatibility
affects: [phase-02-ingest-backbone, hyperdrive, rust-worker, planetscale]

tech-stack:
  added: []
  patterns: [per-request Hyperdrive client, tokio-postgres query_typed_one, source contract test]

key-files:
  created:
    - apps/worker/src/db.rs
    - apps/worker/tests/db_ping_contract.rs
  modified:
    - apps/worker/src/lib.rs
    - apps/worker/wrangler.toml

key-decisions:
  - "Used upstream rust-postgres tokio-postgres rev 35a85bdbfeeac465e092950f65a10d9192418175 with `query_typed_one`; the unavailable devsnek fork was not needed for this smoke test."
  - "Kept Router-facing handler signature as `worker::Request`, matching workers-rs 0.8.1 and Plan 01.5-02's verified API correction."
  - "Used `wrangler tail --format=pretty --search pg` for log-boundary verification because JSON tail output includes Cloudflare request metadata by design."

patterns-established:
  - "`db_ping`: `ctx.env.get_binding::<Hyperdrive>(\"DB\")` -> `hyperdrive.connect()` -> `Config::connect_raw(socket, NoTls)` -> `spawn_local(connection)` -> `query_typed_one` -> JSON response."
  - "TDD contract test reads Rust worker source to guard the Hyperdrive smoke wiring without touching the TypeScript scaffold."

requirements-completed: [BACK-08, INGE-08]

duration: 13 min
completed: 2026-04-30
---

# Phase 01.5 Plan 03: GET /db-ping Smoke Test Summary

**Live Cloudflare Rust Worker smoke route proving PlanetScale staging connectivity through Hyperdrive and upstream tokio-postgres**

## Performance

- **Duration:** 13 min
- **Started:** 2026-04-30T22:13:51Z
- **Completed:** 2026-04-30T22:26:22Z
- **Tasks:** 3
- **Files modified:** 4 tracked files

## Accomplishments

- Replaced the `/db-ping` 501 stub with `apps/worker/src/db.rs`, a per-request Hyperdrive + tokio-postgres smoke handler.
- Configured the real Cloudflare Hyperdrive binding id `9e97e64d73c945cab3548a0dceb05c4b` without committing any PlanetScale connection URL.
- Deployed `bloclawd-worker-staging` with Wrangler 4.87.0 and verified:
  - `GET /` -> HTTP 200, body `ok`
  - `GET /db-ping` -> HTTP 200, body `{"ok":true}` across repeated requests
- Verified final Worker upload size: 886.40 KiB raw / 351.87 KiB gzip; local WASM size: 872,335 bytes.

## Task Commits

Each source/config task was committed atomically:

1. **Task 1: Configure Hyperdrive binding id** - `0f3bf74` (chore)
2. **Task 2 RED: Add failing db-ping contract test** - `8e9b5bc` (test)
3. **Task 2 GREEN: Implement Hyperdrive db-ping route** - `11b9ddd` (feat)
4. **Task 2/3 fix: Use pooler-safe db-ping query after live smoke exposed prepared-statement failure** - `d9904d4` (fix)
5. **Task 3: Deploy + live smoke verification** - no code commit; external verification captured here.

## Files Created/Modified

- `apps/worker/src/db.rs` - Per-request Hyperdrive connection, tokio-postgres `connect_raw`, spawned connection future, `SELECT 1` smoke response.
- `apps/worker/src/lib.rs` - Adds `mod db;`, routes `.get_async("/db-ping", db::db_ping)`, removes 501 stub.
- `apps/worker/tests/db_ping_contract.rs` - Source contract test for db-ping wiring and forbidden-pattern checks.
- `apps/worker/wrangler.toml` - Replaces Hyperdrive id placeholder with real binding id.

## Decisions Made

- `tokio-postgres` pin: upstream `rust-postgres/rust-postgres` rev `35a85bdbfeeac465e092950f65a10d9192418175`, features `["js"]`, `default-features = false`.
- RESEARCH.md Open Question #4 outcome: upstream `tokio-postgres` is viable for this smoke test when using `query_typed_one`. `query_one` produced `UnexpectedMessage` on repeated Hyperdrive requests.
- The first deploy succeeded and the first `/db-ping` returned `{"ok":true}`, but repeated live smoke found the `query_one` pooler bug. After switching to `query_typed_one`, repeated `/db-ping` requests passed.

## Verification

All executable checks passed:

- `cargo build --workspace --locked`
- `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`
- `cargo clippy -p bloclawd-worker --target wasm32-unknown-unknown --locked -- -D warnings`
- `cargo test -p bloclawd-worker --test db_ping_contract --locked`
- `cd apps/worker && worker-build --release`
- `wc -c apps/worker/build/worker/index.wasm` -> `872335`
- `cargo test -p pow --locked`
- `pnpm --filter @bloclawd/worker test`
- `git diff --exit-code apps/worker/src/pow.ts apps/worker/src/types.ts apps/worker/package.json apps/worker/tsconfig.json apps/worker/vitest.config.ts apps/worker/test/`
- `git grep -nE 'console_log!.*(event_id|nonce|cf-connecting-ip|elapsed|conn_string|password|hyperdrive)' -- apps/worker/src/` returned no matches
- `grep -nE 'static [A-Z_]+:|lazy_static|OnceCell|thread_local!' apps/worker/src/db.rs` returned no matches
- `grep -n staging-hyperdrive-id-from-cf-dashboard apps/worker/wrangler.toml` returned no matches
- `grep -nE '^id = "[0-9a-f-]{32,36}"' apps/worker/wrangler.toml` returned `id = "9e97e64d73c945cab3548a0dceb05c4b"`
- `wrangler deploy` deployed version `e9804add-f729-48fe-9a5f-e20ff8ed5f65`
- `curl https://bloclawd-worker-staging.<account-subdomain>.workers.dev/` -> `ok`
- `curl https://bloclawd-worker-staging.<account-subdomain>.workers.dev/db-ping` -> `{"ok":true}` repeatedly
- `wrangler tail bloclawd-worker-staging --format=pretty --search pg` during `/db-ping` produced no `pg` console log lines on clean shutdown

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Switched live smoke query from `query_one` to `query_typed_one`**
- **Found during:** Task 3 live smoke
- **Issue:** Initial deploy succeeded and first `/db-ping` returned `{"ok":true}`, but a repeated request during tail verification returned 500 with `pg query: Error { kind: UnexpectedMessage, cause: None }`.
- **Fix:** Replaced `client.query_one(...)` with `client.query_typed_one(...)`, which upstream tokio-postgres documents as suitable where prepared statements are unsupported.
- **Files modified:** `apps/worker/src/db.rs`, `apps/worker/tests/db_ping_contract.rs`
- **Verification:** Rebuilt, redeployed, and repeated `/db-ping` returned HTTP 200 + `{"ok":true}`.
- **Committed in:** `d9904d4`

**2. [Rule 3 - Blocking] Kept `Request` instead of planned `HttpRequest`**
- **Found during:** Task 2 implementation
- **Issue:** Plan text still referenced `HttpRequest`, but Plan 01.5-02 verified workers-rs 0.8.1 Router handlers require `worker::Request`.
- **Fix:** Implemented `pub async fn db_ping(_req: Request, ctx: RouteContext<()>) -> Result<Response>`.
- **Files modified:** `apps/worker/src/db.rs`
- **Verification:** `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`; `cargo clippy -p bloclawd-worker --target wasm32-unknown-unknown --locked -- -D warnings`
- **Committed in:** `11b9ddd`

**3. [Rule 3 - Blocking] Mirrored worker-build WASM to the plan's expected size-check path**
- **Found during:** Task 2/phase verification
- **Issue:** `worker-build 0.8.1` emits `build/index_bg.wasm`, while the plan checks `build/worker/index.wasm`.
- **Fix:** Copied the generated ignored artifact to `apps/worker/build/worker/index.wasm` before running the size check.
- **Files modified:** none tracked; ignored build artifact only
- **Verification:** `wc -c apps/worker/build/worker/index.wasm` -> `872335`
- **Committed in:** n/a

---

**Total deviations:** 3 auto-fixed (1 bug, 2 blocking)
**Impact on plan:** The final deployed smoke route works. The main behavioral change is positive: upstream tokio-postgres works with `query_typed_one`, reducing dependency on the unavailable devsnek fork.

## Issues Encountered

- `gsd-sdk` is unavailable in this workspace (`node_modules/@gsd-build/sdk` missing and no `gsd-sdk` on PATH), so automated STATE/ROADMAP updates could not run.
- Pre-existing dirty planning/spec docs were left untouched and unstaged.
- `wrangler tail --format=json` includes Cloudflare request metadata such as headers and edge timing by design. The log-boundary check used `--format=pretty --search pg` to observe only the Worker `console_log!` path relevant to this plan.

## Authentication Gates

None. Wrangler deploy and tail were authenticated in the current environment.

## Known Stubs

None.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: public-network-db-smoke | `apps/worker/src/db.rs` | New public `GET /db-ping` route opens a database connection through Hyperdrive. It runs constant `SELECT 1`, takes no request input, logs no secrets or event context, and does not cache clients. |

## Next Phase Readiness

Phase 2 can build ingest endpoints on the Rust Worker using the same Hyperdrive `connect_raw` + `spawn_local` pattern, but should prefer `query_typed_*` APIs over `query_*` when going through Hyperdrive.

## Self-Check: PASSED

- Created files listed in this summary exist on disk.
- Task commits `0f3bf74`, `8e9b5bc`, `11b9ddd`, and `d9904d4` exist in git history.
- Verification commands listed above passed after the final fix commit.

---
*Phase: 01.5-worker-rust-migration-inserted*
*Completed: 2026-04-30*
