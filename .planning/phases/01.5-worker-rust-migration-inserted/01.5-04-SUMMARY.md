---
phase: 01.5-worker-rust-migration-inserted
plan: 04
subsystem: backend
tags: [rust, workers-rs, pow, ci, wasm, ts-rs]

requires:
  - phase: 01.5-worker-rust-migration-inserted
    provides: Shared event-schema crate, Rust Worker scaffold, Hyperdrive db-ping route, and pre-cut-over bilingual PoW gate
provides:
  - Atomic Rust-only Worker cut-over commit
  - Deleted TypeScript Worker scaffold and spec/enums.json
  - Rust-only pow.yml gate with rust-test, fixture-drift, log-boundary, size-check, and all-pass jobs
  - Compile-time pow::verify witness in the Rust Worker
affects: [phase-02-ingest-backbone, phase-03-rust-cli, pow-ci, rust-worker]

tech-stack:
  added: []
  patterns: [single Rust PoW verifier, ts-rs drift gate, log-boundary grep gate, WASM size budget gate]

key-files:
  created: []
  modified:
    - .github/workflows/pow.yml
    - apps/worker/src/lib.rs
    - pnpm-lock.yaml
    - apps/worker/package.json
    - apps/worker/src/pow.ts
    - apps/worker/src/types.ts
    - apps/worker/test/fixtures.test.ts
    - apps/worker/test/pow.test.ts
    - apps/worker/tsconfig.json
    - apps/worker/vitest.config.ts
    - spec/enums.json

key-decisions:
  - "Landed the D-31 cut-over as one atomic commit after human checkpoint approval."
  - "Kept 01.5-04 summary separate from source/config commit so the cut-over remains independently revertible."
  - "Did not rerun the heavy gate stack after approval because the staged diff was already checkpoint-verified and unchanged."

patterns-established:
  - "D-31 atomic flip: delete obsolete TypeScript Worker sources and collapse CI in one commit."
  - "Rust Worker references crates/pow::verify through a compile-time witness before Phase 2 wires runtime POST /event verification."

requirements-completed: [BACK-05, BACK-07, INGE-03, SPEC-04, INGE-11]

duration: 1 min continuation
completed: 2026-04-30
---

# Phase 01.5 Plan 04: Rust-Only Worker Cut-Over Summary

**Atomic Rust-only Worker cut-over deleting the TypeScript scaffold and replacing bilingual PoW CI with Rust, ts-rs drift, log-boundary, and WASM-size gates**

## Performance

- **Duration:** 1 min continuation after checkpoint approval
- **Started:** 2026-04-30T22:43:40Z
- **Completed:** 2026-04-30T22:44:41Z
- **Tasks:** 4
- **Files modified:** 11 tracked files in the cut-over commit

## Accomplishments

- Committed the approved staged cut-over as one D-31 atomic revision: `aff84b6e2e050fda4adeb1a6b3ea84af97ff0459`.
- Deleted the TypeScript Worker package, tests, config, and `spec/enums.json`.
- Collapsed `.github/workflows/pow.yml` to Rust-only CI with `rust-test`, `fixture-drift`, `log-boundary`, `size-check`, and `all-pass`.
- Added the `pow::verify` compile-only witness in `apps/worker/src/lib.rs`.
- Preserved the pre-existing dirty docs/spec files as unstaged, unrelated work.

## Checkpoint Approval

Task 4 was a blocking human-verify checkpoint. The continuation was resumed with user response `approved`, and the staged diff was committed without changing it.

## Task Commits

The plan intentionally lands Tasks 1-3 in one atomic cut-over commit after Task 4 approval:

1. **Tasks 1-4: D-31 atomic Rust-only cut-over** - `aff84b6` (feat)

This summary is committed separately from the source/config cut-over.

## Files Created/Modified

- `.github/workflows/pow.yml` - Removes `ts-test`, `pnpm`, `vitest`, and Node setup; adds ts-rs drift, log-boundary, and WASM size gates.
- `apps/worker/src/lib.rs` - Adds `use pow::verify;` plus a typed compile-only witness.
- `pnpm-lock.yaml` - Regenerated after removing `apps/worker/package.json`.
- `apps/worker/package.json` - Deleted TypeScript package manifest.
- `apps/worker/src/pow.ts` - Deleted TypeScript PoW verifier.
- `apps/worker/src/types.ts` - Deleted TypeScript types.
- `apps/worker/test/fixtures.test.ts` - Deleted TypeScript fixture tests.
- `apps/worker/test/pow.test.ts` - Deleted TypeScript PoW tests.
- `apps/worker/tsconfig.json` - Deleted TypeScript config.
- `apps/worker/vitest.config.ts` - Deleted Vitest config.
- `spec/enums.json` - Deleted; `crates/event-schema/src/enums.rs` is now source of truth.

## Verification

Verification was completed before the checkpoint and approved by the user. This continuation did not rerun the heavy gate stack because the staged diff was unchanged after approval.

Pre-checkpoint verification recorded for this cut-over:

- `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`
- `cargo clippy -p bloclawd-worker --target wasm32-unknown-unknown --locked -- -D warnings`
- `pnpm install --frozen-lockfile`
- `cargo test -p event-schema --locked`
- `cargo test -p pow --locked`
- `cargo run -p xtask --quiet --locked -- gen-fixtures --check`
- `cargo test --features ts-export -p event-schema --locked`
- `git diff --exit-code apps/web/src/generated/`
- `worker-build --release` with WASM size below `2621440` bytes
- `git grep -nE 'console_log!.*(event_id|nonce|cf-connecting-ip|elapsed|password|conn_string)' apps/worker/ crates/` returned no forbidden matches

Checkpoint continuation checks performed before commit:

- `git diff --cached --name-status` matched only the 01.5-04 cut-over files.
- `git status --short` showed unrelated dirty docs/spec files unstaged.
- `git log -1 --stat` after commit showed the full cut-over in `aff84b6`.

## Required Output Notes

- **Atomic cut-over SHA:** `aff84b6e2e050fda4adeb1a6b3ea84af97ff0459`
- **WASM size:** pre-checkpoint size gate passed under `2621440` bytes. The continuation did not receive a PR/merge job transcript or rerun `worker-build`.
- **Unexpected pnpm dependencies:** none reported by the pre-checkpoint verification; `pnpm-lock.yaml` was regenerated and `@bloclawd/worker` was removed with the deleted package.
- **CI green confirmation:** user approved the checkpoint in this local continuation. No PR URL or GitHub Actions run output was provided to this executor.

## Decisions Made

- Used the user checkpoint response as the blocking approval to commit the staged D-31 cut-over.
- Committed only staged cut-over files first, then created this summary as a separate docs commit.
- Left `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `CLAUDE.md`, and `spec/*.md` dirty but unstaged per resume instructions.

## Deviations from Plan

None - plan executed exactly as resumed. No source/config edits were made by this continuation before the atomic commit.

## Issues Encountered

- `gsd-sdk` is unavailable in this workspace (`node_modules/@gsd-build/sdk` missing and no `gsd-sdk` on PATH), so SDK state/roadmap automation was not run.
- The continuation did not include a PR URL or GitHub Actions transcript, so this summary records checkpoint approval and local pre-checkpoint verification rather than external CI evidence.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Threat Flags

None. This plan removes TypeScript verifier surface and adds CI gates; it does not add a new network endpoint, auth path, file access path, or database trust boundary.

## Next Phase Readiness

Plan 01.5-05 can rewrite docs/spec references against the Rust-only Worker state and cite the atomic cut-over commit `aff84b6e2e050fda4adeb1a6b3ea84af97ff0459`.

## Self-Check: PASSED

- Summary file exists on disk.
- Atomic cut-over commit `aff84b6` exists in git history.
- Post-cut-over deletion list in `aff84b6` is intentional and limited to the TypeScript Worker scaffold plus `spec/enums.json`.
- Unrelated dirty docs/spec files remained unstaged before the summary commit.

---
*Phase: 01.5-worker-rust-migration-inserted*
*Completed: 2026-04-30*
