---
phase: 01.5-worker-rust-migration-inserted
verified: 2026-04-30T23:24:14Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
deferred:
  - truth: "Runtime /challenge, /event, and scheduled cron handlers are implemented on the Rust Worker"
    addressed_in: "Phase 2 / Phase 4"
    evidence: "ROADMAP Phase 2 success criteria define GET /challenge and POST /event; ROADMAP Phase 4 success criteria define scheduled aggregation cron."
  - truth: "Runtime Worker and CLI paths consume crates/event-schema for event validation/canonical bytes"
    addressed_in: "Phase 2 / Phase 3"
    evidence: "ROADMAP Phase 2 success criteria validate payloads against crates/event-schema; Phase 3 CLI success criteria consume the shared canonical formatter."
human_verification:
  - test: "Confirm Cloudflare Hyperdrive binding provenance"
    expected: "Cloudflare binding id 9e97e64d73c945cab3548a0dceb05c4b points to the intended PlanetScale staging branch, with the connection URL stored only in Cloudflare/Hyperdrive and not source control."
    why_human: "The repo and live curl prove the binding works, but Cloudflare and PlanetScale dashboard configuration cannot be inspected from this workspace."
    result: "approved by user on 2026-05-01"
---

# Phase 1.5: Worker Rust Migration Verification Report

**Phase Goal:** Flip the backend Worker stack from legacy TypeScript to Rust, make `crates/event-schema` the shared Rust type source with ts-rs bindings, delete former JSON/TS schema/verifier artifacts, collapse CI to Rust plus ts-rs/log/size gates, smoke-test Rust Worker `/db-ping`, and rewrite docs/specs for downstream Rust-only backend work.
**Verified:** 2026-04-30T23:24:14Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `apps/worker/` is rebuilt as a Rust Worker crate and the legacy TypeScript Worker scaffold is gone. | VERIFIED | `apps/worker/Cargo.toml` declares `name = "bloclawd-worker"`, `crate-type = ["cdylib"]`, `worker = 0.8.1`; root `Cargo.toml` includes `apps/worker`; `find apps/worker -name '*.ts'` returned no files; `spec/enums.json` is absent. |
| 2 | `crates/event-schema` is the canonical Rust type/JCS crate and emits checked-in TypeScript bindings. | VERIFIED | `crates/event-schema` exports `EventPayload`, `TokenCounts`, `Model`, `Tier`, `Harness`, `Region`, and `canonical_bytes`; `.cargo/config.toml` sets `TS_RS_EXPORT_DIR`; `cargo test --features ts-export -p event-schema --locked` passed and `git diff --exit-code -- apps/web/src/generated/` passed. |
| 3 | Former enum JSON and TypeScript schema/verifier sources are deleted; docs/specs point at Rust + generated TS bindings. | VERIFIED | `spec/enums.json` absent; `apps/worker/src/pow.ts`, `types.ts`, package/config/test TS files absent; stale-reference grep outside historical `.planning/phases/` records returned no `spec/enums.json`, `apps/worker/src/pow.ts`, `vitest run pow`, `Hono`, `mysql2`, or `zod` hits. |
| 4 | Worker consumes shared Rust PoW verifier and CI is Rust-only plus ts-rs/log/size gates. | VERIFIED | `apps/worker/src/lib.rs` has typed compile-time witness for `pow::verify`; `.github/workflows/pow.yml` has `rust-test`, `fixture-drift`, `log-boundary`, `size-check`, `all-pass`; no `ts-test` or `vitest` in workflow; `cargo test -p pow --locked` and `cargo run -p xtask --quiet --locked -- gen-fixtures --check` passed. |
| 5 | Documentation reflects Rust-only backend and Rust source of truth. | VERIFIED | `CLAUDE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/research/STACK.md`, `.planning/research/ARCHITECTURE.md`, `spec/event-schema.md`, `spec/pow-v1.md`, and `spec/payload-canonical.md` name `workers-rs`, `crates/event-schema`, and `apps/web/src/generated/`; Phase 4 SC#2 drops the R2 enum manifest. |
| 6 | Staging Rust Worker smoke test passes end-to-end. | VERIFIED | Live read-only curl: `/` returned `ok` with HTTP 200; `/db-ping` returned `{"ok":true}` with HTTP 200. Code path opens `Hyperdrive`, uses `connect_raw`, spawns the connection future, runs `query_typed_one("SELECT 1::int4 AS one")`, drops the client, and returns JSON. |

**Score:** 6/6 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | Runtime `/challenge`, `/event`, and scheduled cron handlers. | Phase 2 / Phase 4 | Phase 2 success criteria define `GET /challenge` and `POST /event`; Phase 4 success criteria define scheduled aggregation cron. Phase 1.5 goal explicitly lands before ingest endpoints ship. |
| 2 | Runtime Worker/CLI consumption of `crates/event-schema` for validation/canonical bytes. | Phase 2 / Phase 3 | `apps/worker` has the path dependency now; Phase 2 wires validation into ingest and Phase 3 wires CLI canonical formatting. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Cargo.toml` | Workspace includes `crates/event-schema`, `apps/worker`, and `ts-rs = "12"` | VERIFIED | Members and workspace dependency present; `cargo metadata --no-deps --locked` includes `event-schema` and `bloclawd-worker`. |
| `.cargo/config.toml` | ts-rs export dir | VERIFIED | `TS_RS_EXPORT_DIR = { value = "apps/web/src/generated", relative = true }`. |
| `crates/event-schema/src/*.rs` | Types, enums, JCS helper, validation | VERIFIED | Closed serde enums with ts-rs derives; `EventPayload`/`TokenCounts` use `deny_unknown_fields`; validation rejects `v != 1` and token fields over 10,000,000. |
| `crates/event-schema/tests/*` | JCS vectors and ts-rs export trigger | VERIFIED | RFC8785 vector loop has 3 input/output pairs; `cargo test -p event-schema --locked` passed 15 tests. |
| `apps/web/src/generated/*.ts` | Generated bindings plus barrel | VERIFIED | `EventPayload`, `TokenCounts`, `Model`, `Tier`, `Harness`, `Region`, and `index.ts` present; no ts-rs drift. |
| `apps/worker/Cargo.toml` | Rust Worker manifest and path deps | VERIFIED | `worker = 0.8.1`, `event-schema` and `pow` path deps, upstream `tokio-postgres` rev pin, `cdylib`. |
| `apps/worker/src/lib.rs` | Worker entrypoint, router, pow witness | VERIFIED | `#[event(start)]`, `#[event(fetch)]`, `Router::new()`, `/`, `/db-ping`, and typed `pow::verify` witness present. |
| `apps/worker/src/db.rs` | Hyperdrive db-ping implementation | VERIFIED | Uses `get_binding::<Hyperdrive>("DB")`, `connect_raw`, `spawn_local`, `query_typed_one`, `Response::from_json`, and no cached client state. |
| `apps/worker/wrangler.toml` | Worker build/deploy config | VERIFIED | `compatibility_date = "2026-04-01"` with comment, `placement.mode = "smart"`, Hyperdrive `DB`, two RateLimiter bindings, worker-build command, no compatibility flags. |
| `.github/workflows/pow.yml` | Rust-only CI plus drift/log/size gates | VERIFIED | `ts-test`/`vitest` removed; ts-rs drift, log-boundary, size-check, fixture-drift, and all-pass jobs present. |
| `spec/*.md`, `.planning/*.md`, `CLAUDE.md` | Rust-only doc rewrite | VERIFIED | Active docs/specs point to Rust crate and generated TS bindings; historical phase records excluded from stale-reference grep. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.cargo/config.toml` | `apps/web/src/generated/` | `TS_RS_EXPORT_DIR` | WIRED | `cargo test --features ts-export -p event-schema --locked` emitted bindings; generated dir had zero diff. |
| `crates/event-schema/src/enums.rs` | TypeScript enum bindings | ts-rs derives | WIRED | Generated `Model.ts`, `Tier.ts`, `Harness.ts`, and `Region.ts` contain the expected string unions. |
| `apps/worker/src/lib.rs` | `apps/worker/src/db.rs` | `mod db;` and `.get_async("/db-ping", db::db_ping)` | WIRED | Contract test and source inspection confirm stub removed. |
| `apps/worker/src/db.rs` | Cloudflare Hyperdrive + tokio-postgres | `get_binding::<Hyperdrive>`, `connect_raw`, `spawn_local`, `query_typed_one` | WIRED | Source matches pattern; live `/db-ping` returned `{"ok":true}`. |
| `apps/worker/src/lib.rs` | `crates/pow` | typed `pow::verify` witness | WIRED | Compile-time const proves signature reachability; runtime POST route is deferred to Phase 2. |
| `.github/workflows/pow.yml` | ts-rs/log/WASM gates | workflow jobs | WIRED | `rust-test` includes ts-rs drift; separate `log-boundary` and `size-check` jobs gate PRs. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `apps/worker/src/db.rs` | `one` | PlanetScale staging via Hyperdrive, `SELECT 1::int4 AS one` | Yes | FLOWING - live `/db-ping` returned HTTP 200 `{"ok":true}`. |
| `apps/web/src/generated/*.ts` | generated TS type declarations | Rust types with `#[derive(TS)]` and `TS_RS_EXPORT_DIR` | Yes | FLOWING - ts-rs emission reran and generated dir stayed clean. |
| `crates/event-schema/src/payload.rs` | validation errors | serde closed enums + `EventPayload::validate()` | Yes | FLOWING - unit tests cover unknown fields, bad version, and oversized token field with field name. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Event schema tests pass | `cargo test -p event-schema --locked` | 15 tests passed | PASS |
| ts-rs emission has no drift | `cargo test --features ts-export -p event-schema --locked`; `git diff --exit-code -- apps/web/src/generated/` | Tests passed; no diff | PASS |
| PoW invariant still passes | `cargo test -p pow --locked` | 11 tests passed | PASS |
| Worker contract test passes | `cargo test -p bloclawd-worker --locked` | `db_ping_contract` passed | PASS |
| Fixture drift gate passes | `cargo run -p xtask --quiet --locked -- gen-fixtures --check` | `OK` | PASS |
| Workspace builds | `cargo build --workspace --locked` | Passed | PASS |
| pnpm lock is valid after TS Worker package deletion | `pnpm install --frozen-lockfile` | Passed | PASS |
| Worker WASM stays under 2.5 MiB | `worker-build --release`; `wc -c apps/worker/build/index_bg.wasm` | 872,335 bytes | PASS |
| Live Worker root responds | `curl .../` | `ok`, HTTP 200 | PASS |
| Live db-ping responds | `curl .../db-ping` | `{"ok":true}`, HTTP 200 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BACK-05 | 01.5-02, 01.5-04 | Rust Worker crate, wrangler Rust build, TS scaffold deleted | SATISFIED with deferred route scope | Rust crate and wrangler verified; TS scaffold deleted. `/challenge`, `/event`, and cron are explicitly Phase 2/4 roadmap work. |
| BACK-06 | 01.5-01 | `crates/event-schema` canonical types/JCS + ts-rs bindings | SATISFIED with deferred runtime consumers | Crate exports verified; TS bindings verified. CLI and runtime ingest use are Phase 3/2. |
| BACK-07 | 01.5-04 | Worker consumes `crates/pow`; TS verifier deleted; CI Rust-only | SATISFIED with deferred runtime POST call | `pow::verify` compile witness exists; TS verifier deleted; CI collapsed. Runtime call lands with `POST /event` in Phase 2. |
| BACK-08 | 01.5-03 | Staging Rust Worker db-ping through Hyperdrive | SATISFIED | Live curl returned `{"ok":true}` HTTP 200; source uses Hyperdrive and typed query. |
| BACK-04 | 01.5-02, 01.5-05 | Compatibility date, placement, worker-build | SATISFIED | wrangler has pinned date/comment, `placement.mode = smart`, worker-build command. |
| INGE-03 | 01.5-04, 01.5-05 | PoW verifier is Rust `crates/pow` | SATISFIED with deferred runtime POST call | TS verifier gone; typed `pow::verify` witness compiles. Runtime `/event` deferred to Phase 2. |
| INGE-07 | 01.5-01, 01.5-05 | Payload validation via `crates/event-schema` | SATISFIED with deferred Worker route use | Structs/enums/validation tests verified. Actual ingest handler use deferred to Phase 2. |
| INGE-08 | 01.5-03, 01.5-05 | Per-request Hyperdrive/tokio-postgres client | SATISFIED | `db.rs` uses local client, `spawn_local`, no static cache; live db-ping passes. |
| INGE-10 | 01.5-02, 01.5-05 | RateLimiter bindings | SATISFIED with deferred runtime use | `RL_CHALLENGE` and `RL_EVENT` wrangler bindings present; runtime handlers are Phase 2. |
| INGE-11 | 01.5-04, 01.5-05 | No sensitive Worker logs; CI grep gate | SATISFIED with warning | Forbidden grep returned no matches and workflow gate exists. Review warning remains: raw DB error strings can reach logs outside the source grep. |
| SPEC-04 | 01.5-04, 01.5-05 | CI Rust PoW + fixture + ts-rs + log + size gates | SATISFIED with warning | Workflow has required gates and no TS job. Review warning remains: CI does not run all new crate tests/clippy. |
| SPEC-05 | 01.5-01, 01.5-05 | RFC 8785 JCS crate and conformance docs/tests | SATISFIED with warning | `serde_jcs = 0.2`, `canonical_bytes`, and RFC8785 tests verified. Review warning remains: docs overstate current `canonical_bytes` use by `pow`/`xtask`. |
| AGGR-12 | 01.5-05 | No R2 enum manifest; SPA uses ts-rs bindings | SATISFIED | REQUIREMENTS and ROADMAP say no R2 enum manifest; generated `Model/Tier/Harness/Region` files exist. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/worker/src/db.rs` | 23, 29, 35, 43 | Raw `tokio_postgres::Error` values formatted/logged | WARNING | Advisory review WR-01: could expose operational DB details in Worker logs even though forbidden token grep passes. |
| `apps/worker/Cargo.toml` | 26 | `[profile.release]` in non-root package | WARNING | Cargo warns this profile is ignored. Root release profile exists and size check passes, but worker-specific settings are dead config. |
| `.github/workflows/pow.yml` | 40 | CI omits worker contract tests and worker/event-schema clippy | WARNING | Advisory review WR-03: local tests pass, but CI does not enforce every new crate test/clippy path. |
| `spec/payload-canonical.md` | 12 | Docs overstate current shared helper consumption | WARNING | Advisory review WR-04: `crates/pow` and `xtask` still call `serde_jcs::to_vec` directly, while docs say consumers use `crates/event-schema::canonical_bytes`. |

### Human Verification Completed

### 1. Cloudflare Hyperdrive Binding Provenance

**Test:** In Cloudflare/PlanetScale dashboards, confirm Hyperdrive binding id `9e97e64d73c945cab3548a0dceb05c4b` points to the intended PlanetScale staging branch and the connection string exists only inside Cloudflare Hyperdrive.
**Expected:** Binding target is correct; no PlanetScale connection URL or password is committed or pasted into repo config.
**Result:** Approved by user on 2026-05-01.

### Gaps Summary

No blocker gaps found against Phase 1.5 goal. Automated code, CI, docs, generated bindings, deletion, and live smoke checks passed. External Cloudflare/PlanetScale binding provenance was approved by the user on 2026-05-01.

---

_Verified: 2026-04-30T23:24:14Z_
_Verifier: the agent (gsd-verifier)_
