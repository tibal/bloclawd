---
phase: 01.5-worker-rust-migration-inserted
plan: 02
subsystem: backend
tags: [rust, workers-rs, wasm, cloudflare-worker, hyperdrive, ratelimit]

requires:
  - phase: 01.5-worker-rust-migration-inserted
    provides: Shared event-schema crate and workspace dependency patterns from Plan 01
provides:
  - Rust `apps/worker` Cargo crate named `bloclawd-worker`
  - workers-rs Router scaffold with `GET /` returning `ok` and `GET /db-ping` returning 501
  - Cloudflare `wrangler.toml` with pinned compatibility date, smart placement, Hyperdrive, and RateLimiter bindings
  - Worker WASM build path verified under 2.5 MiB
affects: [phase-01.5, phase-02-ingest-backbone, cloudflare-worker, rust-worker]

tech-stack:
  added: [worker 0.8.1, worker-build 0.8.1, tokio-postgres 0.7.17, wasm-bindgen-futures, console_error_panic_hook, url]
  patterns: [workspace Rust worker crate, workers-rs Router entrypoint, ignored worker-build artifacts]

key-files:
  created:
    - apps/worker/Cargo.toml
    - apps/worker/.gitignore
    - apps/worker/src/lib.rs
    - apps/worker/wrangler.toml
  modified:
    - Cargo.toml
    - Cargo.lock

key-decisions:
  - "Used upstream rust-postgres tokio-postgres v0.7.17 tag rev because the planned devsnek fork was not fetchable."
  - "Kept the literal nodejs_compat string out of wrangler.toml to satisfy the plan's security grep, while preserving the compatibility rationale in different wording."
  - "Used workers-rs 0.8.1 Router Request signatures instead of the plan's HttpRequest handler template because the actual API requires worker::Request."

patterns-established:
  - "Rust Worker crate lives beside the unchanged TypeScript scaffold under apps/worker."
  - "worker-build artifacts stay ignored under apps/worker/build/ and apps/worker/target/."

requirements-completed: [BACK-05, BACK-04, INGE-10]

duration: 23 min
completed: 2026-04-30
---

# Phase 01.5 Plan 02: Rust Worker Scaffold Summary

**Cloudflare Rust Worker crate with workers-rs Router, pinned deployment config, declared Hyperdrive/RateLimiter bindings, and verified sub-2.5 MiB WASM output**

## Performance

- **Duration:** 23 min
- **Started:** 2026-04-30T21:34:09Z
- **Completed:** 2026-04-30T21:57:00Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added `apps/worker` as a Rust workspace member while leaving the existing TypeScript scaffold byte-identical.
- Added `#[event(start)]` panic hook setup and `#[event(fetch)]` Router serving `GET /` as `ok`.
- Added intentional `GET /db-ping` 501 stub for Plan 01.5-03 Hyperdrive smoke test.
- Added `wrangler.toml` with pinned compatibility date, smart placement, Hyperdrive binding, and two RateLimiter bindings.
- Verified worker WASM output at 345,239 bytes, below the 2,621,440-byte budget.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create worker crate manifest and workspace member** - `3527e2e` (feat)
2. **Task 2: Create workers-rs Router scaffold** - `3a624e3` (feat)
3. **Task 3: Create wrangler deployment config** - `ef3061c` (feat)

## Files Created/Modified

- `apps/worker/Cargo.toml` - Rust Worker crate manifest with `worker = 0.8.1`, path deps on `event-schema` and `pow`, and rev-pinned `tokio-postgres`.
- `apps/worker/.gitignore` - Ignores reproducible `build/` and `target/` outputs.
- `apps/worker/src/lib.rs` - workers-rs event handlers and Router stub.
- `apps/worker/wrangler.toml` - Cloudflare Worker build/deploy config and binding declarations.
- `Cargo.toml` - Adds `apps/worker` to workspace members.
- `Cargo.lock` - Locks workers-rs, tokio-postgres, and transitive Worker dependencies.

## Decisions Made

- Used upstream `rust-postgres/rust-postgres` tag `tokio-postgres-v0.7.17` rev `35a85bdbfeeac465e092950f65a10d9192418175` because `devsnek/rust-postgres` returned `Repository not found` and could not be fetched by Cargo.
- Preserved the no-Node-compatibility rationale in `wrangler.toml` without the forbidden `nodejs_compat` literal so the automated security grep remains meaningful.
- Used `worker::Request` in Router-facing signatures; `worker::HttpRequest` does not match `Router::run` or `Router::get_async` in `worker 0.8.1`.

## Verification

All executable plan-level checks passed:

- `cargo build --workspace --locked`
- `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`
- `worker-build --release`
- `wc -c apps/worker/build/worker/index.wasm` -> `345239`
- `cargo test -p pow --locked`
- `cargo run -p xtask --quiet --locked -- gen-fixtures --check`
- `pnpm --filter @bloclawd/worker test`
- `git diff --exit-code apps/worker/src/pow.ts apps/worker/src/types.ts apps/worker/package.json apps/worker/tsconfig.json apps/worker/vitest.config.ts apps/worker/test/`
- `git grep -nE 'console_log!' apps/worker/src/` returned no matches
- `grep -E 'nodejs_compat' apps/worker/wrangler.toml` returned no matches
- The line above `compatibility_date` is a `#` comment with the compatibility rationale
- `cargo clippy -p bloclawd-worker --target wasm32-unknown-unknown --locked -- -D warnings`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced inaccessible devsnek fork with upstream rust-postgres rev**
- **Found during:** Task 1
- **Issue:** `git ls-remote https://github.com/devsnek/rust-postgres.git unnamed-statement` failed with GitHub auth/not-found behavior, and direct GitHub refs reported `Repository not found`.
- **Fix:** Pinned upstream `rust-postgres/rust-postgres` tag `tokio-postgres-v0.7.17` by rev. This keeps the same `tokio-postgres` crate and `js` feature while restoring reproducible locked builds.
- **Files modified:** `apps/worker/Cargo.toml`, `Cargo.lock`
- **Verification:** `cargo metadata --no-deps --locked --format-version 1`; `cargo build --workspace --locked`
- **Committed in:** `3527e2e`

**2. [Rule 3 - Blocking] Added temporary lib stub before Task 2**
- **Found during:** Task 1
- **Issue:** Cargo cannot load a `cdylib` package without `src/lib.rs`, but Task 1 acceptance required `cargo metadata` before Task 2 created the real file.
- **Fix:** Added a temporary compile stub in Task 1, then replaced it with the workers-rs entrypoints in Task 2.
- **Files modified:** `apps/worker/src/lib.rs`
- **Verification:** `cargo metadata --no-deps --locked --format-version 1`; `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`
- **Committed in:** `3527e2e`, replaced in `3a624e3`

**3. [Rule 1 - Bug] Corrected workers-rs Router request types**
- **Found during:** Task 2
- **Issue:** The plan template used `HttpRequest` for Router handlers, but `worker 0.8.1` requires `worker::Request` for `Router::run` and `get_async` handlers.
- **Fix:** Changed `fetch` and `db_ping` signatures to use `Request`.
- **Files modified:** `apps/worker/src/lib.rs`
- **Verification:** `cargo check -p bloclawd-worker --target wasm32-unknown-unknown --locked`; `cargo clippy -p bloclawd-worker --target wasm32-unknown-unknown --locked -- -D warnings`
- **Committed in:** `3a624e3`

**4. [Rule 3 - Blocking] Installed newer Rust toolchain for worker-build CLI**
- **Found during:** Task 3
- **Issue:** `cargo install worker-build@0.8.1` resolved `time 0.3.47`, which requires Rust 1.88.0; the workspace default Rust was 1.86.0.
- **Fix:** Installed Rust 1.88.0 and installed `worker-build 0.8.1` with `cargo +1.88.0 install -q worker-build@0.8.1`. Repository code still verified with normal locked Cargo commands.
- **Files modified:** none
- **Verification:** `worker-build --version` -> `0.8.1`; `worker-build --release`
- **Committed in:** n/a

**5. [Rule 3 - Blocking] Mirrored worker-build WASM to the plan's expected path**
- **Found during:** Task 3
- **Issue:** `worker-build 0.8.1` emits `build/index_bg.wasm`; the plan's verification checks `build/worker/index.wasm`.
- **Fix:** Mirrored the generated WASM to the ignored compatibility path before the size check.
- **Files modified:** none tracked; generated ignored artifact only
- **Verification:** `wc -c apps/worker/build/worker/index.wasm` -> `345239`
- **Committed in:** n/a

**6. [Rule 2 - Missing Critical] Resolved contradictory nodejs_compat instructions in favor of security grep**
- **Found during:** Task 3
- **Issue:** The plan both required a comment mentioning the literal `nodejs_compat` and required `grep -E 'nodejs_compat' apps/worker/wrangler.toml` to return no matches.
- **Fix:** Wrote the compatibility rationale without the forbidden literal string.
- **Files modified:** `apps/worker/wrangler.toml`
- **Verification:** `grep -E 'nodejs_compat' apps/worker/wrangler.toml` returned no matches; line above `compatibility_date` is a `#` comment.
- **Committed in:** `ef3061c`

---

**Total deviations:** 6 auto-fixed (1 bug, 2 missing/blocking source fixes, 3 tooling/verification blockers)
**Impact on plan:** Rust Worker scaffold is complete and verified. Future Plan 01.5-03 must account for upstream `tokio-postgres` instead of the unavailable devsnek fork.

## Issues Encountered

- The task-level `tdd="true"` flags conflicted with the plan truth that every commit must remain green. RED was captured through failing checks instead of committing broken tests.
- Cargo warns that `[profile.release]` inside a non-root workspace package is ignored. The WASM size gate still passed at 345,239 bytes, so no root release-profile change was made in this plan.
- `gsd-sdk` is unavailable in this workspace (`node_modules/@gsd-build/sdk` missing and no `gsd-sdk` on PATH), so automated STATE/ROADMAP updates could not run.
- Pre-existing dirty planning/spec docs were left untouched per execution context.

## User Setup Required

None for committed source. Local execution of `cargo install worker-build@0.8.1` may require Rust 1.88.0 or newer because of current transitive dependency MSRV.

## Known Stubs

- `apps/worker/src/lib.rs:21` - `TODO(01.5-03)` intentionally marks the `/db-ping` 501 stub for the next plan.
- `apps/worker/src/lib.rs:22` - `Response::error("not implemented", 501)` is the intentional `/db-ping` placeholder.
- `apps/worker/wrangler.toml:14` - `<staging-hyperdrive-id-from-cf-dashboard>` is intentionally left for Plan 01.5-03 operator setup.

## Next Phase Readiness

Plan 01.5-03 can add the Hyperdrive smoke test on top of `apps/worker/src/lib.rs`, using the declared `DB` binding and the existing `GET /db-ping` stub as the replacement point.

## Self-Check: PASSED

- Created files listed in this summary exist on disk.
- Task commits `3527e2e`, `3a624e3`, and `ef3061c` exist in git history.
- Verification commands listed above passed after all task commits.

---
*Phase: 01.5-worker-rust-migration-inserted*
*Completed: 2026-04-30*
